# GitHub Actions - Автоматический деплой

## 🚀 Как работает

При каждом пуше в ветку `main` с изменениями в папке `worker/` автоматически происходит деплой на Cloudflare Workers.

## 🔧 Настройка

### 1. Получить Cloudflare API Token

1. Зайди на https://dash.cloudflare.com/profile/api-tokens
2. Нажми **"Create Token"**
3. Используй шаблон **"Edit Cloudflare Workers"**
4. Или создай Custom Token с правами:
   - **Account** → **Workers Scripts** → **Edit**
   - **Zone** → **Workers Routes** → **Edit** (если нужно)
5. Скопируй созданный токен (он показывается только один раз!)

### 2. Добавить токен в GitHub Secrets

1. Открой репозиторий на GitHub
2. Перейди в **Settings** → **Secrets and variables** → **Actions**
3. Нажми **"New repository secret"**
4. Название: `CLOUDFLARE_API_TOKEN`
5. Значение: вставь скопированный токен
6. Нажми **"Add secret"**

### 3. Проверить работу

1. Сделай любое изменение в `worker/index.js`
2. Закоммить и запушь:
   ```bash
   git add .
   git commit -m "Test auto-deploy"
   git push
   ```
3. Открой вкладку **Actions** в репозитории GitHub
4. Увидишь процесс деплоя в реальном времени

## 📊 Что триггерит деплой

Деплой запускается только при изменениях в:
- `worker/**` - любые файлы в папке worker
- `.github/workflows/deploy.yml` - файл workflow

Изменения в `frontend/` НЕ триггерят деплой worker.

## ⏱️ Время деплоя

Обычно занимает **1-2 минуты**:
- ~30 сек: Установка зависимостей
- ~20 сек: Деплой на Cloudflare
- ~10 сек: Проверки и финализация

## ✅ Успешный деплой

После успешного деплоя увидишь:
- ✅ Зеленая галочка в GitHub Actions
- 🚀 Сообщение "Worker deployed successfully"
- 📝 Информация о версии и времени

## ❌ Если деплой не работает

### Проблема: "Error: Authentication error"
**Решение:** Проверь правильность токена в GitHub Secrets

### Проблема: "Error: Unknown command"
**Решение:** Убедись что `wrangler.toml` настроен правильно

### Проблема: "npm ci failed"
**Решение:** Проверь `package.json` и `package-lock.json` в worker/

### Проблема: "This commit does not need to be deployed"
**Решение:** Изменения были не в папке `worker/`, деплой не нужен

## 🔍 Логи

Все логи деплоя доступны в разделе **Actions** на GitHub:
1. Открой репозиторий
2. Вкладка **Actions**
3. Выбери нужный workflow
4. Смотри детальные логи каждого шага

## 🎯 Преимущества

✅ **Автоматизация** - не нужно вручную деплоить  
✅ **Версионность** - каждый деплой связан с коммитом  
✅ **История** - все деплои сохраняются в Actions  
✅ **Надежность** - деплой только при успешной сборке  
✅ **Скорость** - деплой за 1-2 минуты  

## 📝 Примечания

- Деплой происходит только из ветки `main`
- Требуется наличие `package-lock.json` в worker/
- Node.js версия: 18 (можно изменить в workflow)
- Используется `npm ci` (быстрее чем `npm install`)

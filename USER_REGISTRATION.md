# 📝 Автоматическая регистрация пользователей

## 🎯 Как это работает

При запуске бота командой `/start`, бот **автоматически регистрирует** пользователя в Google Sheets.

---

## 🔄 Процесс регистрации

### 1️⃣ **Пользователь отправляет `/start`**

```
Пользователь → /start
```

### 2️⃣ **Бот получает данные**

Бот автоматически получает от Telegram:

- ✅ `telegram_id` - уникальный ID пользователя
- ✅ `username` - юзернейм (если установлен)
- ✅ `first_name` - имя пользователя

### 3️⃣ **Проверка существования**

Бот проверяет таблицу `users` по `telegram_id`:

```javascript
Существует ли пользователь с этим telegram_id?
├── ДА → Пропустить регистрацию
└── НЕТ → Зарегистрировать нового пользователя
```

### 4️⃣ **Регистрация нового пользователя**

Если пользователь **новый**, бот добавляет строку в лист `users`:

| telegram_id | username | first_name | date_registered | bot_started |
|-------------|----------|------------|-----------------|-------------|
| 123456789 | @sergeicz | Илья | 2026-02-04 | TRUE |

---

## 📊 Формат данных

### Колонки в листе `users`:

| Колонка | Описание | Формат | Пример |
|---------|----------|--------|--------|
| **telegram_id** | Telegram ID пользователя | Число | `123456789` |
| **username** | Username с префиксом @ | Текст | `@sergeicz` |
| **first_name** | Имя пользователя | Текст | `Илья` |
| **date_registered** | Дата регистрации | YYYY-MM-DD | `2026-02-04` |
| **bot_started** | Статус запуска бота | Текст | `TRUE` |

### Особенности формата:

✅ **Username:**
- Всегда с префиксом `@`
- Пример: `@sergeicz`
- Если username не установлен: пустая строка `""`

✅ **Date:**
- Формат: `YYYY-MM-DD`
- Пример: `2026-02-04`
- НЕ используется полный ISO: `~~2026-02-04T12:34:56.789Z~~`

✅ **Telegram ID:**
- Всегда число
- Уникален для каждого пользователя
- Не изменяется даже если пользователь меняет username

---

## 🔍 Логи

При регистрации в консоли Cloudflare Workers появляются записи:

### Новый пользователь:

```
[REGISTER] 🆕 New user: 123456789 (@sergeicz)
✅ User registered: 123456789 @sergeicz at 2026-02-04
```

### Существующий пользователь:

```
[REGISTER] ✓ Existing user: 123456789 (@sergeicz)
```

---

## 📋 Структура листа `users`

### Минимальная структура:

Создайте лист `users` с заголовками:

| telegram_id | username | first_name | date_registered | bot_started |
|-------------|----------|------------|-----------------|-------------|

### Дополнительные колонки (опционально):

Вы можете добавить больше колонок для своих нужд:

| telegram_id | username | first_name | last_name | date_registered | bot_started | language_code | is_premium |
|-------------|----------|------------|-----------|-----------------|-------------|---------------|------------|

**Бот заполняет только первые 5 колонок:**
1. telegram_id
2. username
3. first_name
4. date_registered
5. bot_started

Остальные колонки можете заполнять вручную или через другие скрипты.

---

## 🧪 Тестирование

### Проверьте автоматическую регистрацию:

1. **Очистите лист `users`** (оставьте только заголовки)

2. **Отправьте `/start` боту**

3. **Проверьте Google Sheets:**
   - В листе `users` должна появиться новая строка
   - С вашими данными (telegram_id, username, имя)
   - С текущей датой в формате YYYY-MM-DD

4. **Отправьте `/start` ещё раз:**
   - Дублирующая строка НЕ должна появиться
   - Бот пропускает регистрацию для существующих

5. **Проверьте логи Cloudflare:**
   ```
   1-й раз: [REGISTER] 🆕 New user: ...
   2-й раз: [REGISTER] ✓ Existing user: ...
   ```

---

## 💡 Примеры использования данных

### 1. Статистика регистраций

```
Сколько пользователей зарегистрировалось за месяц?
→ Фильтр по date_registered = февраль 2026
→ Посчитать строки
```

### 2. Поиск пользователя

```
Найти пользователя по username:
→ Фильтр: username = @sergeicz
→ Получить telegram_id для других операций
```

### 3. Активные пользователи

```
Сколько пользователей запустили бота?
→ Фильтр: bot_started = TRUE
→ Посчитать строки
```

### 4. Анализ роста базы

```
График регистраций по дням:
→ Группировка по date_registered
→ Подсчет количества на каждую дату
```

---

## 🔄 Обновление данных

### Что НЕ обновляется автоматически:

- **Username** - если пользователь изменил username в Telegram, в таблице останется старый
- **First Name** - аналогично, не обновляется

### Почему так:

- Telegram ID **никогда не меняется** - это главный идентификатор
- Username может меняться часто - обновление при каждом /start может быть избыточным
- Для вашего кейса (партнерские ссылки) важен только факт регистрации

### Если нужно обновлять:

Можно добавить логику обновления. Например:

```javascript
if (existing) {
  // Проверяем изменился ли username
  if (existing.username !== username) {
    // Обновляем строку в Google Sheets
    // (требует дополнительный код для updateSheetRow)
  }
}
```

Но для большинства случаев это не требуется.

---

## 🛡️ Обработка ошибок

### Что происходит при ошибке регистрации:

```javascript
try {
  // Регистрация пользователя
} catch (error) {
  console.error('[REGISTER] Error registering user:', error);
  // Бот продолжает работу - отправляет приветствие
  // НЕ прерывает работу
}
```

**Бот НЕ прерывает работу** если регистрация не удалась:
- Пользователь всё равно получит приветствие
- Ошибка логируется в консоль
- Можно будет зарегистрировать при следующем /start

### Возможные ошибки:

1. **Google Sheets API недоступен**
   - Временная проблема с Google API
   - Пользователь может попробовать позже

2. **Неверные права доступа**
   - Проверьте что сервисный аккаунт имеет доступ к таблице
   - Должен быть "Редактор" или "Владелец"

3. **Неверная структура таблицы**
   - Проверьте что лист называется именно `users`
   - Проверьте что есть все нужные колонки

---

## 📈 Мониторинг

### Как отслеживать регистрации:

1. **Cloudflare Workers Dashboard:**
   - Перейдите в Logs → Real-time Logs
   - Фильтр: `[REGISTER]`
   - Видно все попытки регистрации в реальном времени

2. **Google Sheets:**
   - Сортировка по `date_registered` (по убыванию)
   - Последние регистрации будут сверху

3. **Формулы в Google Sheets:**
   ```
   Всего пользователей: =COUNTA(A2:A)
   Сегодня: =COUNTIF(D2:D, TODAY())
   За неделю: =COUNTIF(D2:D, ">="&TODAY()-7)
   ```

---

## 🚀 Готово к использованию

Система регистрации работает **полностью автоматически**:

✅ Пользователь отправляет /start  
✅ Бот проверяет существование  
✅ Если новый - добавляет в таблицу  
✅ Данные сразу доступны в Google Sheets  
✅ Логи в Cloudflare Workers  

**Никаких дополнительных действий не требуется!** 🎉

---

## 🔧 Настройка листа `users`

### Шаг 1: Создайте лист

1. Откройте вашу Google Таблицу
2. Создайте новый лист с именем **`users`** (точное название!)
3. Добавьте заголовки в **первую строку**:

```
A1: telegram_id
B1: username
C1: first_name
D1: date_registered
E1: bot_started
```

### Шаг 2: Проверьте доступ

1. Убедитесь что сервисный аккаунт имеет доступ к таблице
2. Роль должна быть "Редактор" или выше

### Шаг 3: Тест

1. Отправьте `/start` боту
2. Проверьте что появилась строка с вашими данными

---

## 📝 Итого

✅ **Автоматическая регистрация** при `/start`  
✅ **Проверка дубликатов** по telegram_id  
✅ **Правильный формат** данных (@ для username, YYYY-MM-DD для дат)  
✅ **Детальное логирование** для отладки  
✅ **Устойчивость к ошибкам** - бот работает даже если регистрация не удалась  
✅ **Совместимость** с системой архивации (pidarasy)  

**Всё работает из коробки!** 🚀

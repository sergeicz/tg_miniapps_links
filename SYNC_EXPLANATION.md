# 🔄 Синхронизация данных: Бот ↔️ Google Sheets

## 📊 ТЕКУЩЕЕ СОСТОЯНИЕ

### ⚠️ **Важно понять:**

Бот **НЕ следит автоматически** за активностью пользователей. Синхронизация происходит **только в определённых случаях**.

---

## 🕐 КОГДА ПРОИСХОДИТ ОБМЕН ДАННЫМИ

### 1️⃣ **При запуске бота (`/start`)**

**Направление:** Telegram → Google Sheets

**Что происходит:**
```
Пользователь отправляет /start
     ↓
Бот получает данные от Telegram API
     ↓
Бот читает таблицу users (проверка существования)
     ↓
Если новый → добавляет строку в Google Sheets
```

**Периодичность:** Только когда пользователь сам отправляет `/start`

**Данные не обновляются автоматически!**

---

### 2️⃣ **Во время рассылки (Broadcast)**

**Направление:** Telegram ← Google Sheets → Telegram

**Что происходит:**
```
Админ запускает рассылку
     ↓
Бот читает ВСЮ таблицу users
     ↓
Для каждого пользователя:
  - Пытается отправить сообщение
  - Если ошибка 403/400 → пользователь неактивен
  - Переносит в лист pidarasy
  - Удаляет из листа users
```

**Периодичность:** Только когда вы вручную создаёте рассылку

**Это единственный способ очистки неактивных!**

---

### 3️⃣ **При открытии Mini App**

**Направление:** Google Sheets → Frontend

**Что происходит:**
```
Пользователь открывает Mini App
     ↓
Frontend запрашивает /api/partners
     ↓
Worker читает лист partners из Google Sheets
     ↓
Возвращает данные во Frontend
```

**Периодичность:** Каждый раз при открытии Mini App

**НЕ затрагивает данные о пользователях!**

---

### 4️⃣ **При клике на партнёрскую ссылку**

**Направление:** Frontend → Google Sheets

**Что происходит:**
```
Пользователь кликает на ссылку
     ↓
Frontend отправляет POST /api/click
     ↓
Worker добавляет строку в лист clicks
     ↓
Данные сохранены (telegram_id, url, timestamp)
```

**Периодичность:** При каждом клике

**НЕ проверяет активность пользователя!**

---

## ❌ ЧТО НЕ ПРОИСХОДИТ АВТОМАТИЧЕСКИ

### **Бот НЕ проверяет:**

- ❌ Кто заблокировал бота между рассылками
- ❌ Кто удалил аккаунт Telegram
- ❌ Изменения username пользователей
- ❌ Периодическую активность пользователей

### **Таблица НЕ обновляется:**

- ❌ Нет фоновых задач (background jobs)
- ❌ Нет автоматической очистки
- ❌ Нет периодической синхронизации

---

## 🎯 КАК РАБОТАЕТ ПРОВЕРКА АКТИВНОСТИ

### **Единственный способ - через рассылку:**

```javascript
// Во время broadcast
for (const user of users) {
  try {
    await ctx.api.sendMessage(user.telegram_id, message);
    // ✅ Успешно → пользователь активен
  } catch (error) {
    if (error.error_code === 403) {
      // ❌ Заблокировал бота
      → Переносим в pidarasy
      → Удаляем из users
    } else if (error.error_code === 400) {
      // ❌ Удалил аккаунт / деактивирован
      → Переносим в pidarasy
      → Удаляем из users
    }
  }
}
```

**Проверка происходит ТОЛЬКО когда вы пытаетесь отправить сообщение!**

---

## 💡 ПОЧЕМУ ТАК УСТРОЕНО?

### **Ограничения Telegram Bot API:**

1. **Нет метода "getChatMember" для всех пользователей**
   - Можно проверить только конкретного пользователя
   - Требует отдельный запрос для каждого
   - Rate limit: ~30 запросов/сек

2. **Telegram не уведомляет о блокировке**
   - Бот не получает событие когда пользователь блокирует его
   - Узнаём только при попытке отправить сообщение

3. **Cloudflare Workers - stateless**
   - Нет возможности запускать фоновые задачи без Cron Triggers
   - Каждый запрос - изолированная среда выполнения

---

## 🚀 РЕШЕНИЯ ДЛЯ АВТОМАТИЧЕСКОЙ ПРОВЕРКИ

### **Вариант 1: Cloudflare Workers Cron Triggers** ⭐ **Рекомендуется**

**Что делает:**
- Автоматически запускает проверку по расписанию
- Проверяет всех пользователей
- Обновляет таблицу

**Настройка в `wrangler.toml`:**
```toml
[triggers]
crons = ["0 2 * * *"]  # Каждый день в 2:00 UTC
```

**Код обработчика:**
```javascript
export default {
  async scheduled(event, env, ctx) {
    // Запускается автоматически по расписанию
    const users = await getSheetData(env.SHEET_ID, 'users', accessToken);
    
    for (const user of users) {
      try {
        // Проверяем активность через getChatMember
        await bot.api.getChatMember(user.telegram_id, user.telegram_id);
      } catch (error) {
        if (error.error_code === 403 || error.error_code === 400) {
          // Переносим в архив
          await moveToArchive(user);
        }
      }
    }
  }
}
```

**Плюсы:**
- ✅ Полностью автоматическая проверка
- ✅ Не требует рассылок
- ✅ Настраиваемое расписание

**Минусы:**
- ⚠️ Rate limit Telegram API (30 req/sec)
- ⚠️ Для 1000 пользователей = ~33 секунды проверки
- ⚠️ Дополнительные запросы = расход квоты Cloudflare

---

### **Вариант 2: Проверка во время рассылок** ⭐ **Текущий вариант**

**Что делает:**
- Проверяет активность только когда вы создаёте рассылку
- Автоматически очищает неактивных

**Плюсы:**
- ✅ Уже реализовано
- ✅ Не требует настройки
- ✅ Естественная проверка (при отправке сообщения)

**Минусы:**
- ⚠️ Требует регулярные рассылки
- ⚠️ Между рассылками таблица может быть неактуальна

**Рекомендация:**
- Делайте рассылки хотя бы раз в неделю
- Можно отправлять короткие уведомления для очистки базы

---

### **Вариант 3: Ручная команда для проверки**

**Что делает:**
- Админ может запустить проверку командой `/check_users`

**Пример кода:**
```javascript
bot.command('check_users', async (ctx) => {
  const isAdmin = await checkAdmin(env, ctx.from);
  if (!isAdmin) return;
  
  await ctx.reply('⏳ Начинаю проверку всех пользователей...');
  
  const users = await getSheetData(env.SHEET_ID, 'users', accessToken);
  let inactive = 0;
  
  for (const user of users) {
    try {
      await bot.api.getChatMember(user.telegram_id, user.telegram_id);
    } catch (error) {
      if (error.error_code === 403 || error.error_code === 400) {
        await moveToArchive(user);
        inactive++;
      }
    }
    await new Promise(r => setTimeout(r, 50)); // Rate limit
  }
  
  await ctx.reply(`✅ Проверка завершена!\n📦 Архивировано: ${inactive}`);
});
```

**Плюсы:**
- ✅ Полный контроль
- ✅ Проверка по требованию
- ✅ Не требует рассылок

**Минусы:**
- ⚠️ Нужно запускать вручную
- ⚠️ Rate limit на больших базах

---

## 📊 СРАВНЕНИЕ ВАРИАНТОВ

| Вариант | Автоматизация | Актуальность | Сложность | Стоимость |
|---------|---------------|--------------|-----------|-----------|
| **Cron Triggers** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | 💰💰 |
| **Broadcast** | ⭐⭐ | ⭐⭐⭐ | ⭐ | 💰 |
| **Ручная команда** | ⭐ | ⭐⭐⭐⭐ | ⭐⭐ | 💰 |

---

## 💰 СТОИМОСТЬ ЗАПРОСОВ

### **Cloudflare Workers:**

- Free tier: 100,000 запросов/день
- Cron trigger = 1 запрос при запуске
- Проверка 1000 пользователей = 1000+ запросов к Telegram API

### **Telegram Bot API:**

- Полностью бесплатно
- Rate limit: ~30 запросов/секунду
- Можно проверять ~2,500 пользователей/минута

### **Google Sheets API:**

- Free tier: 60 запросов/минуту на пользователя
- Read/Write операции = 1 запрос каждая

---

## 🎯 РЕКОМЕНДАЦИИ

### **Для небольших баз (<500 пользователей):**

✅ **Текущий вариант (проверка при рассылках)**
- Делайте рассылки раз в неделю
- Этого достаточно для актуальности данных

### **Для средних баз (500-5000 пользователей):**

✅ **Добавьте ручную команду `/check_users`**
- Запускайте раз в месяц для очистки
- Плюс обычные рассылки

### **Для больших баз (>5000 пользователей):**

✅ **Cloudflare Cron Triggers**
- Автоматическая проверка раз в неделю
- Полная актуальность данных
- Требует дополнительной настройки

---

## 🔧 КАК ДОБАВИТЬ АВТОМАТИЧЕСКУЮ ПРОВЕРКУ

### **Хотите автоматическую проверку по расписанию?**

Я могу добавить любой из вариантов:

1. **Cron Triggers** - автоматическая проверка каждый день/неделю
2. **Ручная команда** - `/check_users` для админов
3. **Улучшенная проверка** - во время каждого взаимодействия с ботом

**Какой вариант вам больше подходит?**

---

## 📈 ТЕКУЩАЯ СТАТИСТИКА СИНХРОНИЗАЦИИ

### **Частота обновления данных:**

```
┌─────────────────────────┬────────────────────┬──────────────┐
│ Событие                 │ Частота            │ Направление  │
├─────────────────────────┼────────────────────┼──────────────┤
│ /start                  │ При запуске бота   │ TG → Sheets  │
│ Broadcast               │ Вручную админом    │ Sheets ↔ TG  │
│ Mini App открытие       │ При каждом входе   │ Sheets → App │
│ Клик на ссылку          │ При каждом клике   │ App → Sheets │
│ Проверка активности     │ Только в broadcast │ TG ↔ Sheets  │
└─────────────────────────┴────────────────────┴──────────────┘
```

### **Актуальность данных:**

- **users** - обновляется при /start и broadcast
- **partners** - всегда актуально (читается при каждом открытии)
- **clicks** - всегда актуально (пишется при каждом клике)
- **admins** - всегда актуально (проверяется при каждой команде)
- **pidarasy** - пополняется при broadcast

---

## ✅ ИТОГО

### **Сейчас:**

🔹 Регистрация - при `/start`  
🔹 Проверка активности - при broadcast  
🔹 Очистка неактивных - при broadcast  
🔹 Нет автоматической периодической синхронизации  

### **Чтобы база была актуальной:**

1. ✅ Делайте рассылки регулярно (раз в 1-2 недели)
2. ✅ Или добавьте автоматическую проверку (Cron/команда)
3. ✅ Мониторьте логи для отслеживания проблем

**Хотите добавить автоматическую проверку? Скажите какой вариант предпочитаете!** 🚀

# План внедрения системы подписок для партнеров

## 📊 Структура данных

### 1. Google Sheets таблицы

#### Таблица `tariffs` (тарифы)
- `tariff_id` - уникальный ID тарифа
- `name` - название тарифа
- `price` - цена в рублях
- `duration_days` - длительность в днях (30, 90, 365)
- `description` - описание
- `features` - функции (JSON или текст)
- `active` - активен ли тариф (TRUE/FALSE)

#### Таблица `subscriptions` (подписки партнеров)
- `subscription_id` - уникальный ID подписки
- `partner_title` - название партнера
- `predstavitel_username` - username представителя
- `tariff_id` - ID тарифа
- `start_date` - дата начала
- `end_date` - дата окончания
- `status` - статус (active, expired, cancelled)
- `payment_token` - токен карты для автоплатежей
- `last_payment_date` - дата последнего платежа
- `next_payment_date` - дата следующего платежа
- `notification_5days_sent` - отправлено ли уведомление за 5 дней
- `notification_3days_sent` - отправлено ли уведомление за 3 дня

#### Таблица `payments` (история платежей)
- `payment_id` - ID платежа в ЮMoney
- `subscription_id` - ID подписки
- `amount` - сумма
- `date` - дата платежа
- `status` - статус (success, failed, pending)
- `payment_method` - метод оплаты

## 🔧 ЮMoney интеграция

### Рекуррентные платежи (Saved Payment Method)
1. **Первый платеж** - сохранение карты:
   - Используем `save_payment_method: true`
   - Получаем `payment_token` после успешной оплаты
   
2. **Последующие платежи** - автоматическое списание:
   - Используем сохраненный `payment_token`
   - API endpoint: `https://api.yoomoney.ru/v3/payments`

### Необходимые данные для ЮMoney
- `YOOMONEY_SHOP_ID` - ID магазина
- `YOOMONEY_SECRET_KEY` - секретный ключ
- `YOOMONEY_RETURN_URL` - URL возврата после оплаты

## 🎨 Интерфейсы

### 1. Страница выбора тарифа
- URL: `/frontend/tariffs.html`
- Карточки тарифов с ценами
- Кнопка "Выбрать тариф"
- Редирект на оплату ЮMoney

### 2. Личный кабинет представителя
Добавить в существующий кабинет:
- Текущий тариф
- Дата окончания подписки
- Осталось дней
- Кнопка "Продлить подписку"
- История платежей

### 3. Telegram уведомления
- За 5 дней: "⏰ Ваша подписка заканчивается через 5 дней"
- За 3 дня: "⚠️ Ваша подписка заканчивается через 3 дня"
- В день окончания: "❌ Ваша подписка истекла"

## 🤖 Worker endpoints

### API endpoints для создания:

1. `POST /api/subscription/create`
   - Создание новой подписки
   - Инициализация платежа в ЮMoney

2. `POST /api/subscription/payment-callback`
   - Webhook от ЮMoney
   - Обновление статуса платежа
   - Активация подписки

3. `GET /api/subscription/status`
   - Получение информации о подписке
   - Для отображения в личном кабинете

4. `POST /api/subscription/renew`
   - Ручное продление подписки
   - Редирект на оплату

5. `POST /api/subscription/auto-charge`
   - Автоматическое списание
   - Вызывается cron job'ом

## ⏱️ Cron jobs

### Проверка подписок (каждый день в 10:00)
```javascript
// В scheduled handler
if (hour === 10) {
  await checkSubscriptionExpiry(env);
}
```

Функция `checkSubscriptionExpiry`:
1. Получить все активные подписки
2. Для каждой подписки:
   - Проверить дату окончания
   - За 5 дней: отправить уведомление + пометить flag
   - За 3 дня: отправить уведомление + пометить flag
   - В день окончания: попытка автосписания
   - Если автосписание не удалось: статус = expired

## 🔐 Безопасность

1. **Проверка подписей ЮMoney**
   - Валидация SHA256 hash для webhook'ов
   
2. **Защита API endpoints**
   - Проверка прав доступа
   - Только представитель может видеть свою подписку

3. **PCI DSS**
   - Не храним данные карт
   - Используем токены ЮMoney

## 📝 Этапы внедрения

### Этап 1: Базовая структура ✓
- [x] Создать документацию плана

### Этап 2: Google Sheets структура
- [ ] Создать таблицу `tariffs`
- [ ] Создать таблицу `subscriptions`
- [ ] Создать таблицу `payments`

### Этап 3: ЮMoney интеграция
- [ ] Получить ключи ЮMoney API
- [ ] Создать функции для создания платежа
- [ ] Создать функции для автосписания
- [ ] Создать webhook handler

### Этап 4: API endpoints
- [ ] POST /api/subscription/create
- [ ] POST /api/subscription/payment-callback
- [ ] GET /api/subscription/status
- [ ] POST /api/subscription/renew

### Этап 5: Личный кабинет
- [ ] Добавить отображение подписки
- [ ] Добавить кнопку продления
- [ ] Добавить историю платежей

### Этап 6: Страница тарифов
- [ ] Создать frontend/tariffs.html
- [ ] Дизайн карточек тарифов
- [ ] Интеграция с оплатой

### Этап 7: Уведомления
- [ ] Функция проверки окончания подписок
- [ ] Отправка уведомлений за 5 дней
- [ ] Отправка уведомлений за 3 дня
- [ ] Добавить в cron

### Этап 8: Тестирование
- [ ] Тестовые платежи
- [ ] Проверка автосписаний
- [ ] Проверка уведомлений

## 💰 Примерные тарифы

### Базовый
- 990₽/месяц
- Размещение в Mini App
- Статистика переходов
- Рассылка 1 раз в месяц

### Стандарт
- 2490₽/месяц (-20% при оплате за 3 месяца)
- Всё из Базового
- Рассылки без ограничений
- Приоритетное размещение
- Персональные промокоды

### Премиум
- 8990₽/год (-25% при оплате за год)
- Всё из Стандарт
- Разработка Mini App под ключ
- Настройка рекламы
- Личный менеджер

## 📞 Контакты для настройки

После создания структуры понадобится:
1. Зарегистрировать магазин в ЮMoney
2. Получить `SHOP_ID` и `SECRET_KEY`
3. Настроить webhook URL
4. Добавить секреты в Cloudflare Worker
